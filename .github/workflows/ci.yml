name: CI Pipeline

on:
    pull_request:
        branches:
            - dev
            - main

jobs:
    build:
        runs-on: ubuntu-latest

        steps:
            - name: Checkout code
              uses: actions/checkout@v4

            # ----------------------
            # Node / Angular (future safe)
            # ----------------------
            - name: Setup Node
              uses: actions/setup-node@v4
              with:
                  node-version: "18"

            - name: Placeholder Step
              run: echo "Projects not added yet. CI configured successfully."
