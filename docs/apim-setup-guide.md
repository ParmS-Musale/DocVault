> **Status:** APIM instance created and API imported successfully  
> **Branch:** feature/apim  
> **Last updated:** Feb 2026


## Completed So Far

- APIM instance created (Developer tier)
- DocVault .NET API deployed to Azure App Service
- API imported into APIM using OpenAPI (Swagger)
- Endpoints accessible via APIM gateway
- Backend authentication enforced via Entra ID (JWT)

### Rate limiting, CORS, and caching policies

- Apply rate limiting (10 requests/min per user) in APIM
- Configure CORS policy for Angular frontend origins
- Enable response caching for GET /api/documents
- Update APIM documentation to reflect applied policies"

